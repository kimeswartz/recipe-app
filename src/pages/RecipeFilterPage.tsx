import FilterComponent from "../components/recipe_components/FilterComponent";

const FilterPage = () => {
  return (
    <div>
      <FilterComponent />
    </div>
  );
};

export default FilterPage;